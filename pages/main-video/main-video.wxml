<view class="video-list">
  <block wx:for="{{videoList}}" wx:key="id">
    <video-item class="item"
     video-item="{{item}}"
     bind:tap="onVideoItemTap"
     data-item="{{item}}"
     ></video-item>
  </block>
</view>